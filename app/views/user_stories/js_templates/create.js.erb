<%#=
 * User: nmeylan
 * Date: 01.11.14
 * Time: 17:21
 %>

$('#story-editor-overlay').overlay().close();
var sprint_id = <%= @user_story.sprint_id ? @user_story.sprint_id : -1 %>;
var sprint = $("#sprint-"+sprint_id);
sprint.find('.stories-list').append("<%= escape_javascript render_story(@user_story).html_safe %>");

update_sprint_info(sprint_id);
var input = $('#user-stories-search');
bind_user_story_search_onclick(input);
process_filter(input);