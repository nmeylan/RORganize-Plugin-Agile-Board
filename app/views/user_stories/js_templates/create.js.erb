<%#=
 * User: nmeylan
 * Date: 01.11.14
 * Time: 17:21
 %>

$('#story-editor-overlay').overlay().close();
<% split_class = @sessions[:display_mode].eql?(:split) ? 'splitcontentright' : '' %>
$('#sprint-<%= @sprint.id %>').replaceWith("<%= escape_javascript render_sprint(@sprint, "#{@sprint.id < 0 ? 'backlog ' + split_class : 'sprint' }") %>");
bind_stories_sortable();