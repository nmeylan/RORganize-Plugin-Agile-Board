<%#=
 * User: nmeylan
 * Date: 09.11.14
 * Time: 18:16
 %>
var old_id = <%= old_sprint_id ? old_sprint_id : -1 %>;
var new_id = <%= sprint_id %>;
if(old_id !== new_id){
 update_sprint_info(new_id, '+', <%= points ? points.value : 0 %>);
 update_sprint_info(old_id, '-', <%= points ? points.value : 0 %>);
}
